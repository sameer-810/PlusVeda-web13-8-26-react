/// <reference types="vite/client" />
// Types for `import.meta.env`. The standard Vite scaffold file; it was never
// added here because nothing read the build mode until config.ts started
// warning about unset launch values in dev.
